module subbank {
  import sb = coyotos.SpaceBank;
  print PrimeBank;

  print new Page(PrimeBank);
  def bank = new Bank(PrimeBank);
  print new Page(new Bank(bank));
}

// Local Variables:
// mode:c
// End:
