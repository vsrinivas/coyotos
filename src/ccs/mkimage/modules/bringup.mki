module bringup {
  print new Page(PrimeBank);
  print new GPT(PrimeBank);
  print new Endpoint(PrimeBank);
  print new CapPage(PrimeBank);
  print new Process(PrimeBank);

  def p = new Process(PrimeBank);

  p.faultInfo = 0;
  p.faultCode = 25;		/* startup fault */

  print p;
}

// Local Variables:
// mode:c
// End:
